spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://192.168.1.35:3307/vcs?allowPublicKeyRetrieval=true&useSSL=false&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Taipei
  cloud:
    openfeign:
      okhttp:
        enabled: true
mybatis-plus:
  type-aliases-package: com.tkb
  global-config:
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true
logging:
  level:
    root: INFO
    com.tkb: DEBUG
    com.baomidou.mybatisplus.core.mapper: DEBUG # MyBatis-Plus 映射層
    org.apache.ibatis: DEBUG # 原始 MyBatis 套件
    org.springframework.jdbc: DEBUG # Spring JDBC 相關操作
  pattern:
    dateformat: HH:mm:ss

gitlab:
  url: "http://192.168.1.35/api/v4"
  token: "glpat-Umf7tbJRMxSuetOZe0v_W286MQp1OnkH.01.0w1s5ixl6" #2027/05/18 過期
  projects:
    - name: tkbgoapi
      id: 13
    - name: tkbtv
      id: 3
    - name: go_nuxt
      id: 17
    - name: tkbtv_admin
      id: 33

# MR AI Code Review → N8N Webhook
n8n:
  mr-review:
    # 請依實際 N8N 環境調整（可為內網直連或經 gateway）
    webhook-url: "http://192.168.1.35:8010/n8n-proxy/webhook/78cefc00-1d06-479f-9420-a86570562801"
    auth-header: "Authorization"
    auth-token: "zlbYPGSyoOAhmorUsEougwTUth3psStKidz2"
    callback-base-url: "http://192.168.1.35:8000/api/mr-review/callback"

mr-review:
  # GitLab Webhook 設定的 Secret token（與 X-Gitlab-Token 一致，留空則不驗證）
  gitlab-webhook-secret: "vzbb16DVhTWaJsBeCfzcrpf6"
  # 審核完成後自動在 GitLab MR 討論區留言（Overview 頁可見）
  post-gitlab-comment: true
  scheduler:
    enabled: false
    cron: "0 */10 * * * ?"

knife4j:
  enable: true
springdoc:
  swagger-ui:
    url: /v3/api-docs
    path: /swagger-ui.html

